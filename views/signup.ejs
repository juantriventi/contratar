<!DOCTYPE html>
<html>
<head>
  <title>Sign Up</title>
  <!-- Agrega el enlace a tu archivo CSS de Tailwind -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-md w-1/3 text-center">
    <h1 class="text-2xl font-bold mb-4">Sign Up</h1>
    <form action="/signup" method="post">
      <input type="text" name="username" placeholder="Username" required class="w-full p-2 mb-2 rounded">
      <input type="password" name="password" placeholder="Password" required class="w-full p-2 mb-2 rounded">
      <select name="role" required class="w-full p-2 mb-4 rounded">
        <option value="" disabled selected>Elegi una opcion</option>
        <option value="brindo-servicio">Brindo un servicio</option>
        <option value="busco-empleado">Busco un empleado</option>
      </select>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700">Sign Up</button>
    </form>
    
    <p class="my-4">¿Ya tienes una cuenta? <a href="/login" class="text-blue-500 hover:underline">Inicia sesión</a></p>
    
    <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500"><%= error %></p>
    <% } %>
  </div>
</body>
</html>
