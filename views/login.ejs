<!DOCTYPE html>
<html lang="es">
<head>
  <title>Iniciar sesion</title>
  <!-- Agrega el enlace a tu archivo CSS de Tailwind -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="shortcut icon" href="images/AR.png" type="image/x-icon">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body class="bg-gray-100 h-screen flex items-center justify-center px-10">

  <div class="bg-white p-8 rounded-lg shadow-md w-full sm:w-2/3 md:w-1/2 lg:w-1/3 text-center">
    <h1 class="text-2xl font-bold mb-4">Inicia Sesión</h1>
    <form action="/login" method="POST">
      <input type="text" name="username" placeholder="Usuario" required class="w-full p-2 mb-2 rounded">
      <input type="password" name="password" placeholder="Contraseña" required class="w-full p-2 mb-4 rounded">
      <button type="submit" class="bg-indigo-500 text-white px-4 py-2 rounded hover:bg-indigo-600 w-full">Ingresar</button>
    </form>
    
    <p class="my-2">¿No tienes una cuenta? <a href="/signup" class="text-indigo-500 hover:underline">Crea una cuenta</a></p>
    
    <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500 my-2"><%= error.message %></p>
    <% } %>
  </div>
</body>
</html>
