<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <!-- Agrega el enlace a tu archivo CSS de Tailwind -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-md w-1/3 text-center">
    <h1 class="text-2xl font-bold mb-4">Login</h1>
    <form action="/login" method="POST">
      <input type="text" name="username" placeholder="Username" required class="w-full p-2 mb-2 rounded">
      <input type="password" name="password" placeholder="Password" required class="w-full p-2 mb-4 rounded">
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700">Login</button>
    </form>
    
    <p class="my-4">¿No tienes una cuenta? <a href="/signup" class="text-blue-500 hover:underline">Crea una cuenta</a></p>
    
    <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500"><%= error.message %></p>
    <% } %>
  </div>
</body>
</html>
